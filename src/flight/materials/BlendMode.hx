package flight.materials;

enum abstract BlendMode(Int) from Int {
	var ADD;
	var ALPHA;
	var DARKEN;
	var DIFFERENCE;
	var ERASE;
	var HARDLIGHT;
	var INVERT;
	var LAYER;
	var LIGHTEN;
	var MULTIPLY;
	var NORMAL;
	var OVERLAY;
	var SCREEN;
	var SHADER;
	var SUBTRACT;
}
